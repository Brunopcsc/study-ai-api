spring:
  application:
    name: exam-preparation-ai-backend
  datasource:
    url: jdbc:postgresql://localhost:5433/study_ai_db
    username: admin
    password: admin
    flyway:
      enabled: true
      locations: classpath:db/migration

  ai:
    google:
      genai:
        api-key: ${GEMINI_API_KEY}
        chat:
          options:
            model: gemini-3-flash-preview